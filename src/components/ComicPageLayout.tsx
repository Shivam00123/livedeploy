import React from "react";
import { motion } from "framer-motion";

const ComicPageLayout = () => {
  return (
    <motion.div className="w-screen h-screen bg-blue opacity-60">
      ComicPageLayout
    </motion.div>
  );
};

export default ComicPageLayout;
